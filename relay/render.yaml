services:
  - type: web
    name: raizen-cloud-relay
    runtime: node
    rootDir: relay
    buildCommand: npm install
    startCommand: npm start
    plan: free
    healthCheckPath: /health
    envVars:
      - key: RAIZEN_RELAY_TOKEN
        sync: false
